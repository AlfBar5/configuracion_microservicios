app:
  client_id: validadorlibreria
  grant_type: password
  password: adminlibreria
  urlAuth: http://${HOST_KEYCLOAK}:8080/realms/LibreriaDosRealm/protocol/openid-connect/token
  username: adminlibreria
server:
  port: 7000
eureka:
  instance:
    hostname: ${HOST}
  client:
    register-with-eureka: false
     serviceUrl:
      default-zone: http://${HOST_EUREKA}:8761/eureka
